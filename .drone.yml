---
kind: pipeline
name: default

platform:
  os: linux
  arch: amd64

steps:
  - name: build
    pull: always
    image: spritsail/docker-build

  - name: publish
    pull: always
    image: spritsail/docker-publish
    settings:
      repo: spritsail/roc
      tags:
        - latest
        - "%label org.label-schema.version"
      login: { from_secret: docker_login }
    when:
      branch:
        - master
      event:
        - push

---
kind: pipeline
name: update-readme

steps:
  - name: dockerhub-readme
    pull: always
    image: jlesage/drone-push-readme
    settings:
      username: { from_secret: docker_username }
      password: { from_secret: docker_password }
      repo: spritsail/fivem
    when:
      branch:
        - master
      event:
        - push
---
kind: signature
hmac: 58cb5a4646cdd02c5fdacaf0a8a7512af99f6e77d62b927ffe18352cec464d0c
